import { useEffect, useState } from 'react';
import { api, AudioEvent, Incident, Sensor } from './api/client';
import MapView from './components/MapView'; import StatusBar from './components/StatusBar'; import EventLog from './components/EventLog'; import IncidentPanel from './components/IncidentPanel'; import AddEventForm from './components/AddEventForm';
export default function App(){const [sensors,setSensors]=useState<Sensor[]>([]),[events,setEvents]=useState<AudioEvent[]>([]),[incident,setIncident]=useState<Incident>(); const load=()=>Promise.all([api.sensors(),api.events(),api.incident()]).then(([s,e,i])=>{setSensors(s);setEvents(e);setIncident(i)}); useEffect(()=>{load(); const es=new EventSource('/api/stream'); es.onmessage=()=>load(); return()=>es.close()},[]); return <><StatusBar sensors={sensors} events={events} incident={incident}/><main><MapView sensors={sensors} events={events} incident={incident}/><div className="right"><IncidentPanel incident={incident} onStart={()=>api.start()} onReset={()=>api.reset().then(load)}/><AddEventForm sensors={sensors} onDone={load}/></div></main><EventLog events={events}/></>}
