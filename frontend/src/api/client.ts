export type Sensor={id:number;name:string;lat:number;lon:number;status:string;device_type:string;reliability_score:number};
export type AudioEvent={id:number;sensor_id:number;timestamp:string;lat:number;lon:number;audio_filename?:string;signal_level:number;noise_quality:string;source_noise_type:string;predicted_class:string;confidence:number;processing_status:string};
export type Incident={id:number;started_at:string;status:string;predicted_type:string;confidence:number;trajectory_points:{lat:number;lon:number}[];direction_bearing:number;last_update_at:string;hypothesis_message_count:number};
const json=async<T>(url:string, init?:RequestInit):Promise<T>=>{const r=await fetch(url,{headers:{'Content-Type':'application/json'},...init}); if(!r.ok) throw new Error(await r.text()); return r.json()};
export const api={sensors:()=>json<Sensor[]>('/api/sensors'),events:()=>json<AudioEvent[]>('/api/events'),incident:()=>json<Incident>('/api/incidents/current'),start:()=>json('/api/scenario/start',{method:'POST'}),reset:()=>json('/api/scenario/reset',{method:'POST'}),create:(body:any)=>json<AudioEvent>('/api/events',{method:'POST',body:JSON.stringify(body)}),upload:(fd:FormData)=>fetch('/api/events/upload',{method:'POST',body:fd}).then(r=>r.json())};
